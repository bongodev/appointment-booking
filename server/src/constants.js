const role = ['customer', 'admin', 'provider'];
const appointmentStatus = {
  COMPLETED: 'completed',
  SCHEDULED: 'scheduled',
  CANCELLED: 'cancelled',
};

module.exports = {
  role,
  appointmentStatus,
};
